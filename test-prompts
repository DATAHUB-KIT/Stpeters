!pip install groq
#######################################################3
from groq import Groq
from google.colab import userdata

grokclient = Groq(
    api_key=userdata.get("grok1")
)

#########################################33
response = client.chat.completions.create(
    model="llama-3.3-70b-versatile",
    messages=[
        {"role": "user", "content": "Hi"}
    ]
)

print(response.choices[0].message.content)
###########################################################################3
from PIL import Image
image_path = list(uploaded.keys())[0]

img = Image.open(image_path)

display(img)
##################################################
prompt = """
Analyze this image carefully.

Provide:
1. What is present in the image
2. A detailed description

"""

response = client.models.generate_content(
    model="gemini-3.5-flash",
    contents=[img, prompt]
)

print(response.text)
######################################################
gemini resume screening
##############################################
uploaded = files.upload()
###################################3
filename = list(uploaded.keys())[0]

doc = fitz.open(filename)

resume_text = ""

for page in doc:
    resume_text += page.get_text()

print("Resume Loaded Successfully")
print("-"*50)
print(resume_text[:1500])
###############################################
prompt = f"""
You are an experienced HR Manager, Career Coach and Technical Recruiter.

Analyze the following resume carefully.

Resume:

{resume_text}

Generate a detailed report.

Include the following sections.

# Candidate Summary

# Education

# Experience Level

# Technical Skills

# Suitable IT Job Categories

# Overall Resume Score (100)

Format the response in clean Markdown.
"""
###############################################################
model_name = "nlptown/bert-base-multilingual-uncased-sentiment"
####################################################################
from transformers import AutoTokenizer,AutoModelForSequenceClassification

model = AutoModelForSequenceClassification.from_pretrained(model_name)
tokenizer = AutoTokenizer.from_pretrained(model_name)
############################################################################
classifier = pipeline("sentiment-analysis",
                      model=model,
                      tokenizer=tokenizer)
#########################################################
english to telugu translator using face book model
############################################################
from transformers import AutoTokenizer, AutoModelForSeq2SeqLM

model_name = "facebook/nllb-200-distilled-600M"

tokenizer = AutoTokenizer.from_pretrained(model_name)
model = AutoModelForSeq2SeqLM.from_pretrained(model_name)

text = "Artificial Intelligence is changing the future."

inputs = tokenizer(
    text,
    return_tensors="pt"
)

translated_tokens = model.generate(
    **inputs,
    forced_bos_token_id=tokenizer.convert_tokens_to_ids("tel_Telu"),
    max_new_tokens=100
)

translation = tokenizer.batch_decode(
    translated_tokens,
    skip_special_tokens=True
)

print(translation[0])
############################################################################
